<Window x:Class="rattrapageB4.Views.DoctorFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Médecin" Height="250" Width="400" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Nom:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <TextBox x:Name="txtLastName" Grid.Row="0" Grid.Column="1"/>

        <TextBlock Text="Prénom:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,10,0"/>
        <TextBox x:Name="txtFirstName" Grid.Row="1" Grid.Column="1" Margin="0,5,0,0"/>

        <TextBlock Text="Spécialité:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,10,0"/>
        <ComboBox x:Name="cboSpeciality" Grid.Row="2" Grid.Column="1" Margin="0,5,0,0" DisplayMemberPath="Name"/>

        <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="80" Margin="5" Click="BtnOK_Click"/>
            <Button Content="Annuler" Width="80" Margin="5" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
